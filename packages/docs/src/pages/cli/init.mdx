---
description: Learn how to use the `triplit init` command to add Triplit dependencies and configuration files to an existing project.
---

# `triplit init`

```
triplit init
Initialize a Triplit project

Flags:
--framework, -f Frontend framework helpers to install
    Options: react, svelte, vue, angular
--template, -t Project template to use
    Options: chat
```
