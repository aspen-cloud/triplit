# Welcome to Triplit

Triplit is a full-stack framework for state management and realtime collaboration. It handles:

- Local caching with a fully queryable client-side database
- Durable server-side storage
- Optimistic updates
- Realtime data subscriptions and synchronization with conflict resolution at the field level
- Row level authorization of data
- Rollbacks of failed updates
- Offline read and write capabilities
- Keeping network traffic to a minimum and being performant
- Managing schemas and type definitions

These features are required for any application that aims to be fast and responsive, and are especially important for realtime collaboration.

## Why Triplit?

**Triplit is fast.** By default, every update using triplit occurs optimistically and is saved in a local cache.

**Triplit is flexible.** Triplit supports arbitrary queries over it's cache and handles query invalidation for you. Triplit's cache also ensures your app will not sacrifice UX in poor or offline network conditions.

**Triplit is connected.** Triplit's syncing engine handles the complexity of propagating and merging updates, making it a great fit for quickly adding collaborative features to your application.

**Triplit is developer friendly.** Triplit takes care to be easy to use. It gets rid of the headaches of building a realtime system at all levels of the stack. Additionally, it is designed to work well with Typescript applications, ensuring your type definitions remain up to date as your schema evolves.

The rest of the docs will explain how to use Triplit in your application.
